# ========== LLM & API ==========
openai==1.101.0

# ========== PDF Parsing ==========
# Three-tier fallback strategy for PDF parsing
# Tier 1: pdfplumber (best for tables)
pdfplumber>=0.10.0
# Tier 2: pdfminer.six (best text quality) - installed as dependency of pdfplumber
# Tier 3: pypdfium2 (Chrome engine, ultimate fallback)
pypdfium2>=4.28.0
# Always available: PyMuPDF (final fallback)
pymupdf>=1.24.0
PyPDF2==3.0.1
markitdown[pdf]>=0.0.1

# ========== Web Framework ==========
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# ========== Database ==========
sqlalchemy>=2.0.0
aiofiles>=23.0.0

# ========== Document Export ==========
python-docx>=1.0.0

# ========== Utilities ==========
python-dotenv==1.1.0
tiktoken==0.11.0
pyyaml==6.0.2
requests>=2.31.0

# ========== Accuracy Improvements (v2.0.0) ==========
fuzzywuzzy==0.18.0
python-Levenshtein>=0.25.0  # Updated for Python 3.12+ compatibility

# ========== Testing ==========
pytest>=7.4.0
pytest-asyncio>=0.21.0
pandas>=2.0.3
