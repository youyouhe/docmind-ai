# DeepSeek-OCR-2 Service Dependencies
# Requires CUDA 11.8+ and Python 3.12.9

# Model inference
torch==2.6.0
torchvision==0.21.0
transformers==4.46.3
tokenizers==0.20.3
einops
addict
easydict

# Flash Attention (install separately: pip install flash-attn==2.7.3 --no-build-isolation)
# flash-attn==2.7.3

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# PDF to image conversion
pymupdf>=1.24.0

# Config
python-dotenv>=1.0.0
